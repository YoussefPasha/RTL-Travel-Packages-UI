import * as React from "react";
import Box from "@mui/material/Box";

export default function HomePage() {
  return (
    <Box
      sx={{
        display: "flex",
        marginTop: "100px",
      }}
    >
      Hello
    </Box>
  );
}
